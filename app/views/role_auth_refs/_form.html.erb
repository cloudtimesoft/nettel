<%= form_for(@role_auth_ref) do |f| %>
  <% if @role_auth_ref.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role_auth_ref.errors.count, "error") %> prohibited this role_auth_ref from being saved:</h2>

      <ul>
      <% @role_auth_ref.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role_id %><br>
    <%= f.number_field :role_id %>
  </div>
  <div class="field">
    <%= f.label :auth_id %><br>
    <%= f.number_field :auth_id %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
