<%=render 'head' %>
<h1>Listing admins</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>

      <th>Content</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.name %></td>

        <td><%= admin.content %></td>
        <td><%= admin.status %></td>
        <td><%= link_to '编辑', admin %></td>

        <td><%= link_to 'Destroy', admin, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>





<%= link_to 'New Admin', new_admin_path %>
