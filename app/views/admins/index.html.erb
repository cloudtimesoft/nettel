<%=render 'head' %>

<h2 class="heading">管理员设置</h2>

<table class="table table-hover">
  <thead>
    <tr>
      <th>登录名</th>
      <th>状态</th>
      <th>备注</th>
      <th>操作</th>
    </tr>
  </thead>


    <% @admins.each do |admin| %>
      <tr>
        <td><%= admin.name %></td>
        <td><%= admin.status %></td>
        <td><%= admin.content %></td>
        <td><%= link_to "", admin,class:"icon-pencil" %>
        <%= link_to '', admin, method: :delete, data: { confirm: 'Are you sure?' },class:"icon-remove " %></td>
      </tr>
    <% end %>

</table>

<br>





<%= link_to 'New Admin', new_admin_path %>
